apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: reservation-private-dev
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/Medikong/gitops.git
      targetRevision: HEAD
      path: charts/medikong-service
      helm:
        releaseName: reservation-private-dev
        valueFiles:
          - $values/values/base.yaml
          - $values/values/env/private-dev.yaml
          - $values/values/services/reservation.yaml
          - $values/values/services/private-dev/reservation.yaml
          - $values/values/overrides/private-dev-ha-stable.yaml
          - $values/values/scenarios/istio/reservation-canary-v2.yaml
    - repoURL: https://github.com/Medikong/gitops.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: ticketing-reservation
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
