container:
  resources:
    requests:
      cpu: 380m
      memory: 256Mi
    limits:
      cpu: null
      memory: null

database:
  sqlalchemy:
    poolSize: 20
    maxOverflow: 20
    poolTimeoutSeconds: 10
    poolRecycleSeconds: 1800
