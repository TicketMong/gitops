{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "synthetic-traffic.namespace" . | quote }}
  labels:
    app.kubernetes.io/part-of: medikong
    medikong.io/platform: synthetic
    medikong.io/environment: {{ .Values.environment | quote }}
{{- end }}
