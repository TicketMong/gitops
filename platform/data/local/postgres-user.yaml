postgresql:
  databases:
    user:
      name: user-db
      namespace: ticketing-user
      database: user_service
      credentialsSecret:
        create: true
        name: postgres-user-credentials
        scheme: postgres
        sslMode: disable
